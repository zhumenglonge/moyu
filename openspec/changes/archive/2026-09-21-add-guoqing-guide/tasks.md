# Tasks

## 1. 模块骨架

- [x] 1.1 参照 `games/changsha/index.html` 的结构创建 `games/guoqing/index.html`：同套页面骨架（引用 `../../css/style.css`、`../../js/common.js`、本页 `guoqing.js`，全部相对路径），含倒计时容器、四卡挂载容器、返回大厅链接；验证方式：浏览器直开该文件不出现 404 资源
- [x] 1.2 创建 `games/guoqing/guoqing.js`：IIFE + `"use strict"` 空壳，含 `ROUTES` 常量（按 design D1 形状填入 A 衡山 / B 莽山 / C 崀山 / D 湄江四条完整数据：七字段内容、crowd 星级数字、崀山卡片注明可压缩为 3 天的方案、莽山卡含东江湖雾漫小东江与老秃顶云海安排（不泡温泉））；验证方式：数据里不存在武功山、浏阳、张家界及 6 天以上行程

## 2. 页面渲染

- [x] 2.1 实现情报卡渲染器：按 A/B/C/D 顺序遍历 `ROUTES`，每张卡按统一键序输出七字段，crowd 数字转 ★/☆ 星级，"每日安排"按 Day1..N 逐行展示，卡底附"信息为 2026 年国庆前整理，出行前核实"；验证方式：打开页面四卡完整、字段顺序一致、星级可一眼区分
- [x] 2.2 实现国庆倒计时（design D2）：本地日期归一化后与目标年 10 月 1 日求整数天差，节前"距离国庆还有 N 天"/10.1~10.7"假期进行时"/10.8 后"距下次国庆 N 天"三分支；验证方式：用改系统日期或临时覆写取 date 的调试手段分别验证三个分支文案
- [x] 2.3 实现"复制这条路线发群"：每卡按钮绑定 `navigator.clipboard.writeText`，文本格式按 design D3（路线名 + 七字段逐行 + 署名行），成功/失败均按钮文案变化 1.6s 还原；验证方式：点击后粘贴板内容完整、断网或非安全上下文下行显示"❌ 复制失败"

## 3. 全站集成

- [x] 3.1 `css/style.css` 末尾追加 `gq-` 前缀样式段（design D4）：复用大厅配色变量；版式按 design D4 方案 B：移动端单列、≥720px 两列 2×2、max-width 1080px 居中封顶，含顶色条/dl 字段区/时间轴/倒计时三态；验证方式：仅追加、`git diff` 不出现对已有规则的修改行
- [x] 3.2 大厅 `index.html` 新增 🇨🇳「国庆去哪玩」卡片（链接 `games/guoqing/`，玩法描述"四条长沙 3h 圈路线情报卡 + 复制发群"）；验证方式：大厅点击卡片可达攻略页，攻略页返回链接可回大厅
- [x] 3.3 `README.md` 同步：内置游戏表格加一行、目录树加 `guoqing/` 条目；验证方式：README 表格/目录树与 `games/` 实际内容一致

## 4. 验收

- [x] 4.1 本地 `python3 -m http.server 8000` 起服务（端口占用则先换端口），按 specs/guoqing-guide/spec.md 的 5 个 Requirement 共 13 个 Scenario 逐条走查（含 ESC 老板键伪装/恢复、源码内无第三方域名请求、遐想档徽章仅 E~H 四卡出现）；验证方式：全部 Scenario 通过
- [x] 4.2 手机视口（DevTools 模拟 + 真机可选）检查八卡竖屏可读性与复制按钮可点性；验证方式：375px 宽度无横向滚动、按钮不小于可点区域
- [x] 4.3 提交并推送 main，等待 Actions 部署后线上抽验攻略页与倒计时；验证方式：`https://zhumenglonge.github.io/moyu/games/guoqing/` 可访问（推送前先与用户确认）

## 5. 扩展：桂林/云南方向（用户追加；当时口径"范围放宽"，已被任务组 7 的"核心档/遐想档分层"取代）

- [x] 5.1 `guoqing.js` 的 `ROUTES` 追加 E 桂林（阳朔+龙脊，4天3晚，含可压缩方案）与 F 云南（昆明+建水+东川红土地，4天3晚飞机方向）两条完整七字段数据；验证方式：`node --check` 通过，数据无 6 天以上行程
- [x] 5.2 `style.css` gq 段内追加 `.gq-mod-teal`（#35d0c5 复用既有值）与 `.gq-mod-accent2`（--accent-2）两个顶色条；验证方式：六卡色条各不相同
- [x] 5.3 文案同步：攻略页 hint、大厅卡片描述、README 表格均改为六条线口径；验证方式：grep 无"四条/3h 圈"残留（已归档规划文档除外）

## 6. 扩展：贵州/重庆方向（用户追加）

- [x] 6.1 `guoqing.js` 的 `ROUTES` 追加 G 贵州（西江千户苗寨+镇远，3天2晚，凯里高铁约 2~2.5h）与 H 重庆（山城城区，3天2晚，飞机约 1.5h，纯城区零赶路）两条完整七字段数据；验证方式：`node --check` 通过，无 6 天以上行程
- [x] 6.2 `style.css` gq 段内追加 `.gq-mod-brown`（#8a6a3d 复用 .soko-box 既有值）与 `.gq-mod-fog`（#fff 复用按钮文字色）两个顶色条；验证方式：八卡色条各不相同
- [x] 6.3 文案同步：攻略页 hint、大厅卡片描述、README 表格、JS 文件头注释均改为八条线口径；proposal/specs/design 同步为八条（2×4 网格、八线色条清单）；验证方式：grep 活跃文件无"六条"残留

## 7. 核心档/遐想档分层（用户澄清：3h 圈规矩不破，新方向是给群友遐想用的）

- [x] 7.1 `guoqing.js`：E/F/G/H 四条数据加 `tier: "dream"`，渲染器对遐想档卡头追加"遐想档"徽章；hint/头注释改为两档口径（废弃"3.5h 圈豁免"说法）；验证方式：`node --check` 通过，页面上仅 E~H 四卡带徽章
- [x] 7.2 `style.css` gq 段内追加 `.gq-dream`（虚线描边 --accent，复用既有变量）；大厅卡片描述与 README 改为"核心档/遐想档"口径；验证方式：git diff 对已有规则零修改
- [x] 7.3 proposal/specs/design 同步分层定位（proposal 去"豁免"改"两档"；spec 追加两档 Scenario；design 追加 D6 决策）；验证方式：`openspec validate` 通过，grep 无"豁免/3.5h"残留
